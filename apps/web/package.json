{
  "name": "@continue/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "dev:service": "next dev --hostname ${CONTINUE_SERVICE_HOST:-127.0.0.1} --port ${CONTINUE_SERVICE_PORT:-3000}",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@continue/context-engine": "workspace:*",
    "@continue/memory": "workspace:*",
    "@continue/shared": "workspace:*",
    "@continue/workspace": "workspace:*",
    "@next/env": "15.0.0",
    "next": "15.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "@types/react": "^18.3.11",
    "typescript": "^5.6.2"
  }
}
