{
  "name": "@continue/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@continue/context-engine": "workspace:*",
    "@continue/memory": "workspace:*",
    "@continue/screenpipe": "workspace:*",
    "@continue/shared": "workspace:*",
    "@continue/voice": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
